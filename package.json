{
  "name": "cool-arcade",
  "version": "1.0.0",
  "description": "Novel arcade games played with friends",
  "scripts": {
    "runServer": "tsc && node dist/src/app.js",
    "install:client": "cd web && yarn install && npm run build:production && cd ../",
    "build": "tsc && cd web && npm run build && cd ../",
    "postinstall": "tsc && npm run install:client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "socket.io": "^4.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/socket.io": "^3.0.2",
    "typescript": "^4.5.4"
  },
  "engines": {
    "node": "16.13.1"
  }
}
