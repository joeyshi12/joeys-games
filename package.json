{
  "name": "cool-arcade",
  "version": "1.0.0",
  "description": "Novel arcade games played with friends",
  "scripts": {
    "runServer": "tsc && node dist/src/app.js",
    "postinstall": "tsc",
    "heroku-postbuild": "cd web && yarn install && npm run build && cd ../"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/socket.io": "^2.1.10",
    "@types/socket.io-parser": "^3.0.0",
    "typescript": "^4.5.4"
  },
  "engines": {
    "node": "16.13.1"
  }
}
